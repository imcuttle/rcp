{
  "name": "@rcp/util.tocompclass",
  "author": "imcuttle",
  "description": "The utility converting stateless to be component class",
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "scripts": {
    "dist": "tsc",
    "dev": "npm run dist -- -w",
    "prepublishOnly": "npm run dist && npm test && npm run doc",
    "test": "jest",
    "doc": "documentation --github --markdown-toc=false readme index.js -a public -s \"API\" && git commit -am \"chore: update readme\""
  },
  "keywords": [
    "imcuttle",
    "util.tocompclass"
  ],
  "engines": {
    "node": ">=6"
  },
  "main": "index.js",
  "typings": "index.d.js",
  "version": "1.0.1",
  "dependencies": {
    "@rcp/util.displayname": "^1.0.1",
    "@rcp/util.iscompclass": "^1.0.1"
  },
  "devDependencies": {
    "@rcp/_types": "^1.0.1",
    "typescript": "^3.0.3"
  }
}
