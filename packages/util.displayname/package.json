{
  "name": "@rcp/util.displayname",
  "author": "imcuttle",
  "description": "The utility for getting display name",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@rcp/_types": "^1.0.0"
  },
  "license": "MIT",
  "scripts": {
    "dist": "tsc",
    "dev": "npm run dist -- -w",
    "prepublishOnly": "npm test && npm run doc",
    "test": "jest",
    "doc": "documentation --github --markdown-toc=false readme index.js -a public -s \"API\" && git commit -am \"chore: update readme\""
  },
  "keywords": [
    "imcuttle",
    "util.displayname"
  ],
  "engines": {
    "node": ">=6"
  },
  "main": "index.js",
  "typings": "index.d.js",
  "version": "1.0.0",
  "dependencies": {
    "@rcp/util.iscompclass": "^1.0.0"
  }
}
